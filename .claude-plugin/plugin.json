{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "ticket-plugin",
  "displayName": "Ticket Plugin",
  "version": "1.2.0",
  "description": "Per-ticket tracking for Linear / JIRA tickets, with deep planning, PR creation, and ship-it commands. Seven slash commands — /ticket-plugin:start, :plan, :update, :pause, :archive, :pr, and :merge — keep a durable per-ticket plan + findings + progress log outside the repo; investigate the codebase and produce a parallelism-aware plan with optional subagent fanout in git worktrees; open a PR with a pre-commit simplify pass, project tests, and CodeRabbit review polling; and wrap merge-PR + advance-one-workflow-state (NOT auto-Done — review/QA gates are respected) + archive into one confirmed step.",
  "author": {
    "name": "Ian Smith"
  },
  "homepage": "https://github.com/iansmith/ticket-plugin",
  "repository": "https://github.com/iansmith/ticket-plugin",
  "license": "MIT",
  "keywords": [
    "linear",
    "jira",
    "ticketing",
    "productivity",
    "tdd",
    "code-review",
    "agents"
  ]
}
